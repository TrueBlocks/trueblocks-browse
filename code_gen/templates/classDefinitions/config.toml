[settings]
	class = "Config"
	# doc_group = "20-Routes"
	# doc_descr = "block data as returned from the RPC (with slight enhancements)"
	doc_route = "1110-containers"
	ui_route = "1110-config-v"
	attributes = "itemType=types.Chain|embedType=types.Config|noSdk"
	# produced_by = "blocks"
	# contains = "transaction, withdrawal"
	cache_type = "cacheable"
	cache_by = "range"
