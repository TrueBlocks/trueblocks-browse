// This file is auto-generated. Edit only code inside
// of ExistingCode markers (if any).
package app

import (
	coreTypes "github.com/TrueBlocks/trueblocks-core/src/apps/chifra/pkg/types"
	"github.com/wailsapp/wails/v2/pkg/menu"
)

{{range .Structures -}}
func (a *App) {{.UiRouteView}}(cb *menu.CallbackData) {
	{{if .IsHistory -}}
	address := a.GetSelected()
	a.Navigate("/{{.UiRouteLower}}", address.Hex())
	{{- else if .IsWizard -}}
	if a.isConfigured() {
		a.StepWizard(coreTypes.Reset)
	} else {
		a.StepWizard(coreTypes.Next)
	}
	a.Navigate("/{{.UiRouteLower}}", "")
	{{- else -}}
	a.Navigate("/{{if not .IsProject}}{{.UiRouteLower}}{{end}}", "")
	{{end -}}
}

{{end}}
