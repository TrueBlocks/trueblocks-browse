// This file is auto-generated. Edit only code inside
// of ExistingCode markers (if any).
import {
  // Icons
  {{range .Structures}}{{if (eq .Parent "")}}{{.UiRouteName}}Icon,
  {{end}}{{end}}// Views
  {{range .Structures}}{{if (eq .Parent "")}}{{.UiRouteName}}View,
  {{end}}{{end}}} from "@views";

export type Route ={{range .Structures}}{{if eq .Parent ""}}
  | "{{toLower .UiRouteName}}"
{{end}}{{end -}};

export type RouteItem = {
  order: number;
  route: string;
  label: string;
  icon: JSX.Element;
  component: React.ComponentType;
};

export const routeItems: RouteItem[] = [
{{range .Structures}}{{if (eq .Parent "")}}  {
    order: {{.UiRouteNum}},
    route: "{{toLower .UiRouteName}}",
    label: "{{.UiRouteName}}",
    icon: {{.UiRouteName}}Icon,
    component: {{.UiRouteName}}View,
  },
{{end}}{{end -}}
];
