{{/*==============================================================*/}}

{{ $class := .Class }}
{{ $container := print $class "Container" }}
{{ $routeName := .UiRouteName}}
{{ $routeLower := toLower .UiRouteName -}}
{{ $itemType := .ItemType}}
{{ $itemName := .ItemName}}
{{ $members := .Members }}
{{ $needsSdk := .Needs "sdk" }}

{{ $hasSorts := .HasSorts }}

{{ $hasItems := and (gt (len $itemName) 0) true }}
{{ $hasEmbed := and (gt (len .EmbedName) 0) true }}

{{ $isHistory := eq $class "History" }}
{{ $isStatus := eq $class "Status" }}
{{ $isAbi := eq $class "Abi" }}
{{ $isName := eq $class "Name" }}
{{ $isProject := eq $class "Project" }}
{{ $isMonitor := eq $class "Monitor" }}
{{ $isManifest := eq $class "Manifest" }}

{{ $param1 := "wg *sync.WaitGroup, errorChan chan error" }}
{{ $param2 := "" }}
{{ if $isHistory }}
	{{ $param2 = ", address base.Address" }}
{{ end }}

{{ $lockName := print (toLower $class) "Lock" }}

{{ $dataName := $routeLower }}
{{ if $isHistory }}
	{{ $dataName = $routeLower }}
{{ end }}

{{ $appData := print "a." $routeLower }}
{{ if $isHistory }}
	{{ $appData = $routeLower }}
{{ end }}

{{/*==============================================================*/}}
// This file is auto-generated. Edit only code inside
// of ExistingCode markers (if any).
package app

// EXISTING_CODE
// EXISTING_CODE

var {{$lockName}} atomic.Uint32

func (a *App) load{{$routeName}}({{$param1}}{{$param2}}) error {
	defer a.trackPerformance("load{{$routeName}}", false)()
	defer func() {
		if wg != nil {
			wg.Done()
		}
	}()

	if !{{$lockName}}.CompareAndSwap(0, 1) {
		return nil
	}
	defer {{$lockName}}.CompareAndSwap(1, 0)

	// EXISTING_CODE
	// EXISTING_CODE

	if !{{$appData}}.NeedsUpdate() {
		return nil
	}
	updater := {{$appData}}.Updater
	defer func() {
{{ if $isHistory }}	history, _ = a.historyCache.Load(address)
{{end}}		{{$appData}}.Updater = updater
{{ if $isHistory }}	a.historyCache.Store(address, history)
{{end}}	}()
	logger.InfoBY("Updating needed for {{$routeLower}}...")

{{if $needsSdk}}
	opts := sdk.{{$routeName}}Options{
		Globals: a.getGlobals(true /* verbose */),
	}
{{end}}	// EXISTING_CODE
	// EXISTING_CODE
{{if $needsSdk}}	if {{$dataName}}, meta, err := opts.{{$routeName}}List(); err != nil {
		if errorChan != nil {
			errorChan <- err
		}
		return err
	} else if ({{$dataName}} == nil) || (len({{$dataName}}) == 0) {
		err = fmt.Errorf("no {{$dataName}} found")
		if errorChan != nil {
			errorChan <- err
		}
		return err
	} else {
{{end}}		// EXISTING_CODE
		// EXISTING_CODE
{{if $needsSdk}}		a.meta = *meta
		{{$appData}} = types.New{{$container}}(opts.Chain
{{- if $hasItems -}}
, {{$dataName}}{{if $isStatus}}[0].Caches{{end}}{{if $isManifest}}[0].Chunks{{end}}
{{- end -}}
{{- if $hasEmbed -}}
, &{{$dataName}}[0]
{{- end -}}
)
{{end}}		// EXISTING_CODE
		// EXISTING_CODE
{{if $hasSorts -}}
		if err := sdk.Sort{{$routeName}}({{$appData}}.Items, {{$appData}}.Sorts); err != nil {
			a.emitErrorMsg(err, nil)
		}
{{end -}}
		a.emitLoadingMsg(messages.Loaded, "{{$dataName}}")
{{if $needsSdk}}	}
{{end}}
	return nil
}

// EXISTING_CODE
// EXISTING_CODE
