// This file is auto-generated. Edit only code inside
// of ExistingCode markers (if any).
package app

// EXISTING_CODE
// EXISTING_CODE

func (a *App) Fetch{{.Class}}(first, pageSize int) *types.{{.Class}}Container {
	// EXISTING_CODE
	// EXISTING_CODE

{{if .NeedsPaging -}}
	filtered := a.{{.UiRouteLower}}.CollateAndFilter(a.filterMap){{if .HasItems}}.([]{{.ItemFullType}}){{end}}
	first = base.Max(0, base.Min(first, len(filtered)-1))
	last := base.Min(len(filtered), first+pageSize)
	copy, _ := a.{{.UiRouteLower}}.ShallowCopy().(*types.{{.Class}}Container)
	copy.Items = filtered[first:last]
	return copy
{{- else -}}
	_ = a.{{.UiRouteLower}}.CollateAndFilter(a.filterMap)
	copy, _ := a.{{.UiRouteLower}}.ShallowCopy().(*types.{{.Class}}Container)
	return copy
{{- end}}
}
