{{ $noChildren := (eq .Children "") }}
{{ if $noChildren }}
	{{/*==============================================================*/}}
	{{ $class := .Class }}
	{{ $routeName := .UiRouteName }}
	{{ $routeLower := toLower .UiRouteName }}
	{{ $fullType := .ItemFullType }}
	{{ $embedType := .EmbedType }}

	{{ $isProject := eq $class "Project" }}

	{{/*==============================================================*/}}
	// This file is auto-generated. Edit only code inside
	// of ExistingCode markers (if any).
	// EXISTING_CODE
	// EXISTING_CODE

	const columnHelper = createColumnHelper<{{$fullType}}>();

	export const {{$routeName}}TableDef: CustomColumnDef<{{$fullType}}, any>[] = [
		// EXISTING_CODE
		// EXISTING_CODE
	];

	// EXISTING_CODE
	// EXISTING_CODE
{{- end }}
