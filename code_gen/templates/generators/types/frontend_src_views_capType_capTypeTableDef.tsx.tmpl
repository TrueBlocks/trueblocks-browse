{{ $class := .Class }}
{{ $routeName := .UiRouteName }}
{{ $routeLower := toLower .UiRouteName }}
{{ $itemName := .ItemName }}
{{ $embedType := .EmbedType }}

{{ $isSession := eq $class "Session" }}
{{ $isConfig := eq $class "Config" }}
{{ $isProject := eq $class "Project" }}
{{ $isSessConf := or $isSession $isConfig }}

{{ $tableDef := $routeName }}
{{ if $isProject }}
  {{ $tableDef = "base" }}
{{ end -}}

// This file is auto-generated. Edit only code inside
// of ExistingCode markers (if any).
// EXISTING_CODE
// EXISTING_CODE

const columnHelper = createColumnHelper<types.{{$itemName}}>();

export const {{$tableDef}}TableDef: CustomColumnDef<types.{{$itemName}}, any>[] = [
	// EXISTING_CODE
	// EXISTING_CODE
];

// EXISTING_CODE
// EXISTING_CODE
